{
  "name": "pins-scorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first Kegel-Scorer (Vanilla ES-Module, kein Build-Step).",
  "scripts": {
    "test": "node --test",
    "test:e2e": "node tools/e2e-headless.mjs",
    "test:all": "node --test && node tools/e2e-headless.mjs",
    "dev": "python tools/devserver.py 5173"
  }
}
